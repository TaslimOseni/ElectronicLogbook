<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dabinu.app.electroniclogbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.AuthActivity" />
        <activity android:name=".exit.ExitActivity" />
        <activity android:name=".student.StudentActivity" />
        <activity android:name=".dept_supervisor.DeptSupervisorActivity" />
        <activity android:name=".ind_supervisor.IndSupervisorActivity" />
        <activity android:name=".technical_officer.TechnicalOfficerActivity"></activity>
    </application>

</manifest>